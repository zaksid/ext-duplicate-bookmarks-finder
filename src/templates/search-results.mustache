{{# isLoader }}
<div id="loader-wrapper">
    {{> loader }}
</div>
{{/ isLoader }}

{{^ isLoader }}
<div>
    <div class="alert alert-warning" role="alert">
        {{i18n.msg_FoundQty}}<br>
    </div>
</div>

<form id="bookmarks-list">
    {{#hasResults}}
        <button class="mdc-button mdc-button--outlined" type="submit" disabled>
            <span class="mdc-button__ripple"></span>
            <i class="material-icons mdc-button__icon" aria-hidden="true">delete</i>
            <span class="mdc-button__label">{{i18n.btn_deleteSelected}}</span>
        </button>
    {{/hasResults}}

    <div class="bookmarks-list-wrapper">
        {{#cards}}
            {{> card}}
        {{/cards}}
    </div>
</form>
{{/ isLoader }}
